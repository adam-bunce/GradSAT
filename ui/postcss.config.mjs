/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
    // autoprefixer: {} for vendor prefixes which i doubt i will need
  },
};

export default config;
