import { Geist_Mono, Playfair_Display } from "next/font/google";

const playfair = Playfair_Display({ subsets: ["latin"] });
const geist_mono = Geist_Mono({ subsets: ["latin"] });

export { geist_mono, playfair };
